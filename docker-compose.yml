services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: reps
      POSTGRES_PASSWORD: password
      POSTGRES_DB: reps
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - reps-pgdata:/var/lib/postgresql/data

volumes:
  reps-pgdata:
